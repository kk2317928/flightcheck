# External PostgreSQL reachable from this host and Vercel Web.
# Use the same database (not necessarily the same connection pool URL).
DATABASE_URL=postgresql://user:password@db.example.com:5432/flightcheck?sslmode=verify-full
RUN_SEED=true
FLIGHTCHECK_IMAGE_TAG=latest
